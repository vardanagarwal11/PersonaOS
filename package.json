{
  "name": "personaos",
  "version": "1.0.0",
  "private": true,
  "description": "PersonaOS — AI-native economic identity on Stellar. Root dev launcher.",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm --prefix backend start\" \"cross-env PORT=4001 npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
